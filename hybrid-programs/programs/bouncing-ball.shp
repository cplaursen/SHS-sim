Constants {
 g = -9.81;
}

Variables {
 real x;
 real v;
}

SHP {
    x := 10.0;
    v := 1.0;
    loop {
        if x < 0.0 {
             v := -0.8 * v;
             x := 0.0;
         }
        {x' = v, v' = g} dt + {x'=0.0, v'=0.0} dW & x >= 0.0;
    }
}
