module DefBlocks where

-- Blocks = Consts | Vars | Enum | Settings | SHP

-- Consts - replace throughout

-- Vars - declare continuous variables (could do continuous/discrete)

-- Enum - reserve strings for enum type. replace Var "enum" with Enum "enum" throughout

-- Settings - goes into options
